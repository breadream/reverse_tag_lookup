services:
  reverse-tag-lookup:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - reverse-tag-cache:/app/data
    environment:
      RUST_LOG: reverse_tag_lookup=info,tower_http=info

volumes:
  reverse-tag-cache:
